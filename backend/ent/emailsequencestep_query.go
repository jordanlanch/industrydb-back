// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/jordanlanch/industrydb/ent/emailsequence"
	"github.com/jordanlanch/industrydb/ent/emailsequencesend"
	"github.com/jordanlanch/industrydb/ent/emailsequencestep"
	"github.com/jordanlanch/industrydb/ent/predicate"
)

// EmailSequenceStepQuery is the builder for querying EmailSequenceStep entities.
type EmailSequenceStepQuery struct {
	config
	ctx          *QueryContext
	order        []emailsequencestep.OrderOption
	inters       []Interceptor
	predicates   []predicate.EmailSequenceStep
	withSequence *EmailSequenceQuery
	withSends    *EmailSequenceSendQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EmailSequenceStepQuery builder.
func (_q *EmailSequenceStepQuery) Where(ps ...predicate.EmailSequenceStep) *EmailSequenceStepQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *EmailSequenceStepQuery) Limit(limit int) *EmailSequenceStepQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *EmailSequenceStepQuery) Offset(offset int) *EmailSequenceStepQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *EmailSequenceStepQuery) Unique(unique bool) *EmailSequenceStepQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *EmailSequenceStepQuery) Order(o ...emailsequencestep.OrderOption) *EmailSequenceStepQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QuerySequence chains the current query on the "sequence" edge.
func (_q *EmailSequenceStepQuery) QuerySequence() *EmailSequenceQuery {
	query := (&EmailSequenceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(emailsequencestep.Table, emailsequencestep.FieldID, selector),
			sqlgraph.To(emailsequence.Table, emailsequence.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, emailsequencestep.SequenceTable, emailsequencestep.SequenceColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySends chains the current query on the "sends" edge.
func (_q *EmailSequenceStepQuery) QuerySends() *EmailSequenceSendQuery {
	query := (&EmailSequenceSendClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(emailsequencestep.Table, emailsequencestep.FieldID, selector),
			sqlgraph.To(emailsequencesend.Table, emailsequencesend.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, emailsequencestep.SendsTable, emailsequencestep.SendsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EmailSequenceStep entity from the query.
// Returns a *NotFoundError when no EmailSequenceStep was found.
func (_q *EmailSequenceStepQuery) First(ctx context.Context) (*EmailSequenceStep, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{emailsequencestep.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *EmailSequenceStepQuery) FirstX(ctx context.Context) *EmailSequenceStep {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EmailSequenceStep ID from the query.
// Returns a *NotFoundError when no EmailSequenceStep ID was found.
func (_q *EmailSequenceStepQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{emailsequencestep.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *EmailSequenceStepQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EmailSequenceStep entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EmailSequenceStep entity is found.
// Returns a *NotFoundError when no EmailSequenceStep entities are found.
func (_q *EmailSequenceStepQuery) Only(ctx context.Context) (*EmailSequenceStep, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{emailsequencestep.Label}
	default:
		return nil, &NotSingularError{emailsequencestep.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *EmailSequenceStepQuery) OnlyX(ctx context.Context) *EmailSequenceStep {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EmailSequenceStep ID in the query.
// Returns a *NotSingularError when more than one EmailSequenceStep ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *EmailSequenceStepQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{emailsequencestep.Label}
	default:
		err = &NotSingularError{emailsequencestep.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *EmailSequenceStepQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EmailSequenceSteps.
func (_q *EmailSequenceStepQuery) All(ctx context.Context) ([]*EmailSequenceStep, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EmailSequenceStep, *EmailSequenceStepQuery]()
	return withInterceptors[[]*EmailSequenceStep](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *EmailSequenceStepQuery) AllX(ctx context.Context) []*EmailSequenceStep {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EmailSequenceStep IDs.
func (_q *EmailSequenceStepQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(emailsequencestep.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *EmailSequenceStepQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *EmailSequenceStepQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*EmailSequenceStepQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *EmailSequenceStepQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *EmailSequenceStepQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *EmailSequenceStepQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EmailSequenceStepQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *EmailSequenceStepQuery) Clone() *EmailSequenceStepQuery {
	if _q == nil {
		return nil
	}
	return &EmailSequenceStepQuery{
		config:       _q.config,
		ctx:          _q.ctx.Clone(),
		order:        append([]emailsequencestep.OrderOption{}, _q.order...),
		inters:       append([]Interceptor{}, _q.inters...),
		predicates:   append([]predicate.EmailSequenceStep{}, _q.predicates...),
		withSequence: _q.withSequence.Clone(),
		withSends:    _q.withSends.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithSequence tells the query-builder to eager-load the nodes that are connected to
// the "sequence" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *EmailSequenceStepQuery) WithSequence(opts ...func(*EmailSequenceQuery)) *EmailSequenceStepQuery {
	query := (&EmailSequenceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSequence = query
	return _q
}

// WithSends tells the query-builder to eager-load the nodes that are connected to
// the "sends" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *EmailSequenceStepQuery) WithSends(opts ...func(*EmailSequenceSendQuery)) *EmailSequenceStepQuery {
	query := (&EmailSequenceSendClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSends = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SequenceID int `json:"sequence_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EmailSequenceStep.Query().
//		GroupBy(emailsequencestep.FieldSequenceID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *EmailSequenceStepQuery) GroupBy(field string, fields ...string) *EmailSequenceStepGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EmailSequenceStepGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = emailsequencestep.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SequenceID int `json:"sequence_id,omitempty"`
//	}
//
//	client.EmailSequenceStep.Query().
//		Select(emailsequencestep.FieldSequenceID).
//		Scan(ctx, &v)
func (_q *EmailSequenceStepQuery) Select(fields ...string) *EmailSequenceStepSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &EmailSequenceStepSelect{EmailSequenceStepQuery: _q}
	sbuild.label = emailsequencestep.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EmailSequenceStepSelect configured with the given aggregations.
func (_q *EmailSequenceStepQuery) Aggregate(fns ...AggregateFunc) *EmailSequenceStepSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *EmailSequenceStepQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !emailsequencestep.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *EmailSequenceStepQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EmailSequenceStep, error) {
	var (
		nodes       = []*EmailSequenceStep{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withSequence != nil,
			_q.withSends != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EmailSequenceStep).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EmailSequenceStep{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withSequence; query != nil {
		if err := _q.loadSequence(ctx, query, nodes, nil,
			func(n *EmailSequenceStep, e *EmailSequence) { n.Edges.Sequence = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSends; query != nil {
		if err := _q.loadSends(ctx, query, nodes,
			func(n *EmailSequenceStep) { n.Edges.Sends = []*EmailSequenceSend{} },
			func(n *EmailSequenceStep, e *EmailSequenceSend) { n.Edges.Sends = append(n.Edges.Sends, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *EmailSequenceStepQuery) loadSequence(ctx context.Context, query *EmailSequenceQuery, nodes []*EmailSequenceStep, init func(*EmailSequenceStep), assign func(*EmailSequenceStep, *EmailSequence)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*EmailSequenceStep)
	for i := range nodes {
		fk := nodes[i].SequenceID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(emailsequence.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "sequence_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *EmailSequenceStepQuery) loadSends(ctx context.Context, query *EmailSequenceSendQuery, nodes []*EmailSequenceStep, init func(*EmailSequenceStep), assign func(*EmailSequenceStep, *EmailSequenceSend)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*EmailSequenceStep)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(emailsequencesend.FieldStepID)
	}
	query.Where(predicate.EmailSequenceSend(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(emailsequencestep.SendsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.StepID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "step_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *EmailSequenceStepQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *EmailSequenceStepQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(emailsequencestep.Table, emailsequencestep.Columns, sqlgraph.NewFieldSpec(emailsequencestep.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, emailsequencestep.FieldID)
		for i := range fields {
			if fields[i] != emailsequencestep.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withSequence != nil {
			_spec.Node.AddColumnOnce(emailsequencestep.FieldSequenceID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *EmailSequenceStepQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(emailsequencestep.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = emailsequencestep.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EmailSequenceStepGroupBy is the group-by builder for EmailSequenceStep entities.
type EmailSequenceStepGroupBy struct {
	selector
	build *EmailSequenceStepQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *EmailSequenceStepGroupBy) Aggregate(fns ...AggregateFunc) *EmailSequenceStepGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *EmailSequenceStepGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmailSequenceStepQuery, *EmailSequenceStepGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *EmailSequenceStepGroupBy) sqlScan(ctx context.Context, root *EmailSequenceStepQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EmailSequenceStepSelect is the builder for selecting fields of EmailSequenceStep entities.
type EmailSequenceStepSelect struct {
	*EmailSequenceStepQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *EmailSequenceStepSelect) Aggregate(fns ...AggregateFunc) *EmailSequenceStepSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *EmailSequenceStepSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmailSequenceStepQuery, *EmailSequenceStepSelect](ctx, _s.EmailSequenceStepQuery, _s, _s.inters, v)
}

func (_s *EmailSequenceStepSelect) sqlScan(ctx context.Context, root *EmailSequenceStepQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
