// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/jordanlanch/industrydb/ent/emailsequence"
	"github.com/jordanlanch/industrydb/ent/emailsequencestep"
)

// EmailSequenceStep is the model entity for the EmailSequenceStep schema.
type EmailSequenceStep struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Sequence this step belongs to
	SequenceID int `json:"sequence_id,omitempty"`
	// Order of this step in the sequence (1, 2, 3...)
	StepOrder int `json:"step_order,omitempty"`
	// Days to wait before sending (0 = send immediately)
	DelayDays int `json:"delay_days,omitempty"`
	// Email subject line
	Subject string `json:"subject,omitempty"`
	// Email body (supports variables: {{lead_name}}, {{user_name}}, etc.)
	Body string `json:"body,omitempty"`
	// Creation timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the EmailSequenceStepQuery when eager-loading is set.
	Edges        EmailSequenceStepEdges `json:"edges"`
	selectValues sql.SelectValues
}

// EmailSequenceStepEdges holds the relations/edges for other nodes in the graph.
type EmailSequenceStepEdges struct {
	// Sequence holds the value of the sequence edge.
	Sequence *EmailSequence `json:"sequence,omitempty"`
	// Emails sent from this step
	Sends []*EmailSequenceSend `json:"sends,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// SequenceOrErr returns the Sequence value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EmailSequenceStepEdges) SequenceOrErr() (*EmailSequence, error) {
	if e.Sequence != nil {
		return e.Sequence, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: emailsequence.Label}
	}
	return nil, &NotLoadedError{edge: "sequence"}
}

// SendsOrErr returns the Sends value or an error if the edge
// was not loaded in eager-loading.
func (e EmailSequenceStepEdges) SendsOrErr() ([]*EmailSequenceSend, error) {
	if e.loadedTypes[1] {
		return e.Sends, nil
	}
	return nil, &NotLoadedError{edge: "sends"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EmailSequenceStep) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case emailsequencestep.FieldID, emailsequencestep.FieldSequenceID, emailsequencestep.FieldStepOrder, emailsequencestep.FieldDelayDays:
			values[i] = new(sql.NullInt64)
		case emailsequencestep.FieldSubject, emailsequencestep.FieldBody:
			values[i] = new(sql.NullString)
		case emailsequencestep.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EmailSequenceStep fields.
func (_m *EmailSequenceStep) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case emailsequencestep.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case emailsequencestep.FieldSequenceID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sequence_id", values[i])
			} else if value.Valid {
				_m.SequenceID = int(value.Int64)
			}
		case emailsequencestep.FieldStepOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field step_order", values[i])
			} else if value.Valid {
				_m.StepOrder = int(value.Int64)
			}
		case emailsequencestep.FieldDelayDays:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field delay_days", values[i])
			} else if value.Valid {
				_m.DelayDays = int(value.Int64)
			}
		case emailsequencestep.FieldSubject:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field subject", values[i])
			} else if value.Valid {
				_m.Subject = value.String
			}
		case emailsequencestep.FieldBody:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field body", values[i])
			} else if value.Valid {
				_m.Body = value.String
			}
		case emailsequencestep.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EmailSequenceStep.
// This includes values selected through modifiers, order, etc.
func (_m *EmailSequenceStep) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySequence queries the "sequence" edge of the EmailSequenceStep entity.
func (_m *EmailSequenceStep) QuerySequence() *EmailSequenceQuery {
	return NewEmailSequenceStepClient(_m.config).QuerySequence(_m)
}

// QuerySends queries the "sends" edge of the EmailSequenceStep entity.
func (_m *EmailSequenceStep) QuerySends() *EmailSequenceSendQuery {
	return NewEmailSequenceStepClient(_m.config).QuerySends(_m)
}

// Update returns a builder for updating this EmailSequenceStep.
// Note that you need to call EmailSequenceStep.Unwrap() before calling this method if this EmailSequenceStep
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *EmailSequenceStep) Update() *EmailSequenceStepUpdateOne {
	return NewEmailSequenceStepClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the EmailSequenceStep entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *EmailSequenceStep) Unwrap() *EmailSequenceStep {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: EmailSequenceStep is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *EmailSequenceStep) String() string {
	var builder strings.Builder
	builder.WriteString("EmailSequenceStep(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("sequence_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SequenceID))
	builder.WriteString(", ")
	builder.WriteString("step_order=")
	builder.WriteString(fmt.Sprintf("%v", _m.StepOrder))
	builder.WriteString(", ")
	builder.WriteString("delay_days=")
	builder.WriteString(fmt.Sprintf("%v", _m.DelayDays))
	builder.WriteString(", ")
	builder.WriteString("subject=")
	builder.WriteString(_m.Subject)
	builder.WriteString(", ")
	builder.WriteString("body=")
	builder.WriteString(_m.Body)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// EmailSequenceSteps is a parsable slice of EmailSequenceStep.
type EmailSequenceSteps []*EmailSequenceStep
