// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/jordanlanch/industrydb/ent/lead"
	"github.com/jordanlanch/industrydb/ent/leadassignment"
	"github.com/jordanlanch/industrydb/ent/user"
)

// LeadAssignment is the model entity for the LeadAssignment schema.
type LeadAssignment struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ID of the assigned lead
	LeadID int `json:"lead_id,omitempty"`
	// ID of the user who owns this lead
	UserID int `json:"user_id,omitempty"`
	// ID of the user who made the assignment (null for automatic assignments)
	AssignedByUserID *int `json:"assigned_by_user_id,omitempty"`
	// Whether the assignment was automatic or manual
	AssignmentType leadassignment.AssignmentType `json:"assignment_type,omitempty"`
	// Reason for assignment (e.g., 'round-robin', 'location match', 'manual')
	AssignmentReason string `json:"assignment_reason,omitempty"`
	// When the lead was assigned
	AssignedAt time.Time `json:"assigned_at,omitempty"`
	// Whether this is the current assignment (false if reassigned)
	IsActive bool `json:"is_active,omitempty"`
	// Creation timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the LeadAssignmentQuery when eager-loading is set.
	Edges        LeadAssignmentEdges `json:"edges"`
	selectValues sql.SelectValues
}

// LeadAssignmentEdges holds the relations/edges for other nodes in the graph.
type LeadAssignmentEdges struct {
	// Lead holds the value of the lead edge.
	Lead *Lead `json:"lead,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// AssignedBy holds the value of the assigned_by edge.
	AssignedBy *User `json:"assigned_by,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// LeadOrErr returns the Lead value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LeadAssignmentEdges) LeadOrErr() (*Lead, error) {
	if e.Lead != nil {
		return e.Lead, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: lead.Label}
	}
	return nil, &NotLoadedError{edge: "lead"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LeadAssignmentEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// AssignedByOrErr returns the AssignedBy value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LeadAssignmentEdges) AssignedByOrErr() (*User, error) {
	if e.AssignedBy != nil {
		return e.AssignedBy, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "assigned_by"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LeadAssignment) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case leadassignment.FieldIsActive:
			values[i] = new(sql.NullBool)
		case leadassignment.FieldID, leadassignment.FieldLeadID, leadassignment.FieldUserID, leadassignment.FieldAssignedByUserID:
			values[i] = new(sql.NullInt64)
		case leadassignment.FieldAssignmentType, leadassignment.FieldAssignmentReason:
			values[i] = new(sql.NullString)
		case leadassignment.FieldAssignedAt, leadassignment.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LeadAssignment fields.
func (_m *LeadAssignment) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case leadassignment.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case leadassignment.FieldLeadID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field lead_id", values[i])
			} else if value.Valid {
				_m.LeadID = int(value.Int64)
			}
		case leadassignment.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = int(value.Int64)
			}
		case leadassignment.FieldAssignedByUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field assigned_by_user_id", values[i])
			} else if value.Valid {
				_m.AssignedByUserID = new(int)
				*_m.AssignedByUserID = int(value.Int64)
			}
		case leadassignment.FieldAssignmentType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field assignment_type", values[i])
			} else if value.Valid {
				_m.AssignmentType = leadassignment.AssignmentType(value.String)
			}
		case leadassignment.FieldAssignmentReason:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field assignment_reason", values[i])
			} else if value.Valid {
				_m.AssignmentReason = value.String
			}
		case leadassignment.FieldAssignedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field assigned_at", values[i])
			} else if value.Valid {
				_m.AssignedAt = value.Time
			}
		case leadassignment.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				_m.IsActive = value.Bool
			}
		case leadassignment.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LeadAssignment.
// This includes values selected through modifiers, order, etc.
func (_m *LeadAssignment) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryLead queries the "lead" edge of the LeadAssignment entity.
func (_m *LeadAssignment) QueryLead() *LeadQuery {
	return NewLeadAssignmentClient(_m.config).QueryLead(_m)
}

// QueryUser queries the "user" edge of the LeadAssignment entity.
func (_m *LeadAssignment) QueryUser() *UserQuery {
	return NewLeadAssignmentClient(_m.config).QueryUser(_m)
}

// QueryAssignedBy queries the "assigned_by" edge of the LeadAssignment entity.
func (_m *LeadAssignment) QueryAssignedBy() *UserQuery {
	return NewLeadAssignmentClient(_m.config).QueryAssignedBy(_m)
}

// Update returns a builder for updating this LeadAssignment.
// Note that you need to call LeadAssignment.Unwrap() before calling this method if this LeadAssignment
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *LeadAssignment) Update() *LeadAssignmentUpdateOne {
	return NewLeadAssignmentClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the LeadAssignment entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *LeadAssignment) Unwrap() *LeadAssignment {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: LeadAssignment is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *LeadAssignment) String() string {
	var builder strings.Builder
	builder.WriteString("LeadAssignment(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("lead_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.LeadID))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	if v := _m.AssignedByUserID; v != nil {
		builder.WriteString("assigned_by_user_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("assignment_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.AssignmentType))
	builder.WriteString(", ")
	builder.WriteString("assignment_reason=")
	builder.WriteString(_m.AssignmentReason)
	builder.WriteString(", ")
	builder.WriteString("assigned_at=")
	builder.WriteString(_m.AssignedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsActive))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// LeadAssignments is a parsable slice of LeadAssignment.
type LeadAssignments []*LeadAssignment
